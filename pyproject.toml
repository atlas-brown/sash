[project]
name = "sash"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pash_annotations @ git+https://github.com/binpash/annotations.git@more_cmds",
  "setuptools",
  "libdash == 0.3.1",
  "shasta == 0.1.0",
  "shortuuid",
  "pytest",
  "z3-solver>=4.14.1.0",
  "pandas",
]

[project.scripts]
sash = "sash:argmain"
