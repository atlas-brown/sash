{
    "errors": [
        {
            "code": "unbound",
            "shellcheck": {
                "detects": false,
                "where": "${NODE_VERSION}",
                "note": "ShellCheck fails to detect that the variable is referenced without being assigned."
            }
        }
    ]
}
