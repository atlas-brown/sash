{
    "errors": [
        {
            "code": "not_a_command",
	    "note": "todo let's get consensus: I think this is essentially the same error as the `//` command bug (milestone_2/rm_root)",
	    "line": 115,
            "shellcheck": {
                "detects": false,
                "where": "env zsh"
            }
        }
    ]
}
