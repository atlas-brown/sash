{
    "errors": [
        {
            "code": "empty_var",
            "shellcheck": {
                "detects": false,
                "note": "ShellCheck fails to detect the possibility of the `STEAMROOT` variable being empty."
            }
        },
        {
            "code": "del_sys_file",
            "shellcheck": {
                "detects": true,
                "code": "SC2115",
                "where": "rm -rf \"$STEAMROOT/*\"",
                "note": "ShellCheck detects the catastrophic bug and suggests to \"Use ${var:?} to ensure this never expands to / .\""
            }
        }
    ]
}
