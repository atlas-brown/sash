{
    "errors": [
        {
            "code": "del_sys_file",
            "shellcheck": {
                "detects": true,
                "code": "SC2115",
                "where": "rm -rf $SQUID_PIDFILE_DIR/*",
                "note": "ShellCheck detects the catastrophic bug and suggests to \"Use ${var:?} to ensure this never expands to / .\""
            }
        }
    ]
}
