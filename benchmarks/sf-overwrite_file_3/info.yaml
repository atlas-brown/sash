sources:
  - https://stackoverflow.com/questions/49605847/file-copying-in-while-loop-is-executing-for-one-time-only

bugs:
  bug00:
    description: File is accidentally overwritten in every iteration of a loop
    code: data_loss
    shellcheck: null

ground_truths:
  # Base versions
  - path: original.sh
    kind: original
    bugs:
      bug00: { lines: [9] }

  - path: posix.sh
    kind: buggy
    bugs:
      bug00: { lines: [9] }

  - path: fixed.sh
    kind: fixed
    bugs:
      bug00: { regression_lines: [9], shellcheck: null }
