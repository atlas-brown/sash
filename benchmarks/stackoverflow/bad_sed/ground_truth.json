{
    "errors": [
        {
            "code": "wrong_sed_range",
            "shellcheck": {
                "detects": false,
                "where": "`\"1 $n d\"`",
                "note": "ShellCheck fails to detect that the command misses a comma between `1` and `$n`, which yields an incorrect range."
            }
        }
    ]
}
