{
    "errors": [
        {
            "code": "wrong_sed_range",
            "shellcheck": {
                "detects": false,
                "note": "ShellCheck does not detect that the command misses a comma between `1` and `$n`, which yields an incorrect range."
            }
        }
    ]
}
