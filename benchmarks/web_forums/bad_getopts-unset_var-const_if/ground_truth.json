{
    "errors": [
        {
            "code": "bad_command_argument_format",
	    "note": "todo this feels out of scope",
	    "line": 5,
            "shellcheck": {
                "detects": false,
                "where": ":i:x:n",
                "note": "ShellCheck fails to detect that the option string misses a colon after `n`, which erroneously informs `getopts` that `-n` does not take an argument, and thus does not set the variable `$OPTARG`."
            }
        },
        {
            "code": "unbound",
	    "line": 9,
            "shellcheck": {
                "detects": true,
                "code": "SC2153",
                "where": "$OPTARGS",
                "note": "ShellCheck detects the bug and suggests that \"Possible misspelling: OPTARGS may not be assigned. Did you mean OPTARG?\""
            }
        },
        {
            "code": "command_can_only_fail",
	    "line": 16,
	    "note": "the real bug here is that mkdir can only ever fail",
            "shellcheck": {
                "detects": false,
                "where": "mkdir $dirName",
                "note": "ShellCheck fails to detect that the intention is to check if the directory exists, which should be done with `[ -d \"$OPTARG\" ]` instead of `[ -f \"$OPTARG\" ]`, and thus `mkdir` always fails because the directory does not exist."
            }
        }
    ]
}
