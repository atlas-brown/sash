{
    "errors": [
        {
            "code": "unlikely_command",
	    "line": 4,
            "shellcheck": {
                "detects": false,
                "where": "directorytocopy=/Users/myname/Library/Application Support",
                "note": "ShellCheck fails to detect that `Application Support` is supposed to be part of a path but is not quoted, which leads to a command-not-found error since `Support` is treated as an argument."
            }
        },
        {
            "code": "const_cond",
	    "line": 6,
            "shellcheck": {
                "detects": true,
                "code": "SC2154",
                "where": "if [ ! -d \"$directorytocopy\" ]:",
                "note": "ShellCheck detects the bug and warns that `directorytocopy` is referenced but not assigned."
            }
        },
        {
            "code": "dead_code",
	    "line": 10,
            "shellcheck": {
                "detects": false,
                "where": "mkdir -p \"$backuptodirectory\"",
                "note": "ShellCheck fails to detect that the code is dead because it is unreachable due to the previous errors."
            }
        }
    ]
}
