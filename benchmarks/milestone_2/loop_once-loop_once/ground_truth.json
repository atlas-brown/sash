{
    "errors": [
        {
            "code": "loop_once",
            "shellcheck": {
                "detects": false,
                "where": "for directory in \"$DIR\"; do",
                "note": "ShellCheck fails to detect loops that run only once due to a lack of globbing that leads to a single iteration."
            }
        },
        {
            "code": "loop_once",
            "shellcheck": {
                "detects": false,
                "where": "for filename in \"$directory\";",
                "note": "ShellCheck fails to detect loops that run only once due to a lack of globbing that leads to a single iteration."
            }
        }
    ]
}
