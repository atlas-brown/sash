{
    "errors": [
        {
            "code": "unbound",
            "shellcheck": {
                "detects": false,
                "code": "SC2164",
                "note": "Even though ShellCheck suggests to \"Use 'cd ... || exit' or 'cd ... || return' in case cd fails.,\" it does not detect that `ZSH` is unbound/unset."
            }
        },
        {
            "code": "unbound",
            "shellcheck": {
                "detects": false,
                "note": "ShellCheck does not detect that `NORMAL` is unbound/unset."
            }
        },
        {
            "code": "unbound",
            "shellcheck": {
                "detects": false,
                "note": "ShellCheck does not detect that `RB_RESET` is unbound/unset."
            }
        }
    ]
}
