bugs:
- line: 180
  description: Log file is deleted due to improper exit code checking
- line: 203
  description: Log file is deleted due to improper exit code checking

name: "Ubuntu backup manager"

sources:
- https://git.launchpad.net/ubuntu/+source/backup-manager/commit/lib/backup-methods.sh?id=59cdd322f7772d40b6aff25b6720796271f4deba

notes:
- To produce posix.sh, some array loops and references were changed to normal variable
  loops and references, because the arrays in question are not defined in the script anyways.
- The original fix uses PIPESTATUS, which is not POSIX compliant; the fixed version captures
  the exit code of the first command in the pipeline by storing it in a temporary variable

ground_truth:
  errors:
  - line: 180
    line_in_fixed: 181
    code: const_cond
    shellcheck:
      detects: false
      fixed_false_positive: false
      code: SC2181
  # Note: This is not buggy # TODO: Make a variant that is
  # - line: 205
  #   line_in_fixed: 211
  #   code: const_cond
  #   note: This is only const along one execution path
  #   shellcheck:
  #     detects: false
  #     fixed_false_positive: false
  #     code: SC2181
  #     note: Warns about not checking exit codes indirectly; it's a 'style' check
