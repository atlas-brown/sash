sources:
  - https://stackoverflow.com/questions/42844610/restoring-deleted-file-using-bash-in-unix

bugs:
  bug00:
    description: If the first argument is empty, grep expects input from stdin
    code: unexpected_stdin
    shellcheck: null

ground_truths:
  # Base versions
  - path: original.sh
    kind: original
    bugs:
      bug00: { lines: [3, 4] }

  - path: posix.sh
    kind: buggy
    bugs:
      bug00: { lines: [3, 4] }

  - path: fixed.sh
    kind: fixed
    bugs:
      bug00: { regression_lines: [3, 4], shellcheck: null }
