sources:
  - https://github.com/ventoy/Ventoy/blob/master/SQUASHFS/squashfs-tools-4.4/squashfs-tools/build.sh
  - https://github.com/ventoy/Ventoy/issues/3531

bugs:
  bug00:
    description: The script checks if a file exists, and then tries to copy that file even if it doesn't
    code: cmd_expected_path_state
    shellcheck: null

ground_truths:
  # Base versions
  - path: original.sh
    kind: original
    bugs:
      bug00: { lines: [36] }
