2 mandatory arguments in getopts in shell script
 Execution of shell script which needs 2 mandatory arguments
```
-d
```
and
```
-v
```
and 1 optional argument
```
-h
```
not working accordingly
executing the script
```
store.sh
```
like below
```
store.sh -d <directory> -v<version>

```
I tried this like below, but its not working, the script is running with one argument also i.e
```
-d
```
without
```
-v
```
```
while getopts "hd:v:" arg; do
  case "$arg" in
    h) usage 0;;
    d) DIRECTORY="$optarg"  ;;
    v) VERSION="$optarg" ;;
    *) usage 1;;
  esac
done  

```