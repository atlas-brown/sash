How do I use $variable) in case shell script
 I have a command that produces the a|b|c|d|e output and stores it in valid_names.
In the script bellow, instead of $valid_names if I put the following, it works.
```
a|b|c|d|e)

```
But none of the these work:
```
$valid_names)
"$valid_names")
$(printf $valid_names))

```
Please help:
```
#!/bin/sh

valid_names=a|b|c|d|e
printf "Enter name to check: "
while :
do
  read NAME
  case $NAME in
    $valid_names)
      break
      ;;
    *)
      printf "Valid names are $valid_names, enter a valid name: "
  esac
done

```